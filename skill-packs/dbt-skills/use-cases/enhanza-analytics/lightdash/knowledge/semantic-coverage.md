<!-- Generated by scripts/lightdash_context_sync.py — do not hand-edit.
     Regenerate: python3 scripts/use_case_sync.py --use-case enhanza-analytics --stage lightdash -->
# Lightdash semantic coverage

What of this project's semantic layer actually reaches Lightdash, and what
does not. MetricFlow stays the metric's source of truth; the Lightdash CLI
(@lightdash/cli 1.97.0, the external/lightdash submodule pin) translates
the supported subset from `manifest.json` at compile/deploy time. Nothing
below was re-authored as a Lightdash metric — a projection that dropped a
filter or a window would be a wrong number that plans successfully.

## Explore joins derived from relationships tests

- `fortnox_bi_dim_accounts` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_dim_accounts.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_dim_articles` → `fortnox_bi_dim_company` on `${fortnox_bi_dim_articles.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_dim_assets_types` → `fortnox_bi_dim_company` on `${fortnox_bi_dim_assets_types.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_dim_bundle_articles` → `fortnox_bi_dim_articles` on `${fortnox_bi_dim_bundle_articles.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_dim_financial_years` → `fortnox_bi_dim_company` on `${fortnox_bi_dim_financial_years.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_dim_prices` → `fortnox_bi_dim_articles` on `${fortnox_bi_dim_prices.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_dim_supplier_invoice_files` → `fortnox_bi_fact_supplier_invoices` on `${fortnox_bi_dim_supplier_invoice_files.SupplierInvoiceId} = ${fortnox_bi_fact_supplier_invoices.SupplierInvoiceId}` (many-to-one)
- `fortnox_bi_dim_supplier_items` → `fortnox_bi_dim_articles` on `${fortnox_bi_dim_supplier_items.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_dim_supplier_items` → `fortnox_bi_dim_suppliers` on `${fortnox_bi_dim_supplier_items.SupplierId} = ${fortnox_bi_dim_suppliers.SupplierId}` (many-to-one)
- `fortnox_bi_fact_absence_transactions` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_absence_transactions.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_absence_transactions` → `fortnox_bi_dim_employees` on `${fortnox_bi_fact_absence_transactions.EmployeeId} = ${fortnox_bi_dim_employees.EmployeeId}` (many-to-one)
- `fortnox_bi_fact_asset_rows` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_asset_rows.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_asset_rows` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_fact_asset_rows.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_fact_assets` → `fortnox_bi_dim_assets_types` on `${fortnox_bi_fact_assets.AssetTypeId} = ${fortnox_bi_dim_assets_types.AssetTypeId}` (many-to-one)
- `fortnox_bi_fact_assets` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_assets.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_attendance_transactions` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_attendance_transactions.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_attendance_transactions` → `fortnox_bi_dim_employees` on `${fortnox_bi_fact_attendance_transactions.EmployeeId} = ${fortnox_bi_dim_employees.EmployeeId}` (many-to-one)
- `fortnox_bi_fact_budgets` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_budgets.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_budgets` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_budgets.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_budgets` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_fact_budgets.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_fact_contract_rows` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_contract_rows.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_contract_rows` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_contract_rows.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_contract_rows` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_contract_rows.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_contracts` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_contracts.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_contracts` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_contracts.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_employee_schedules` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_employee_schedules.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_employee_schedules` → `fortnox_bi_dim_employees` on `${fortnox_bi_fact_employee_schedules.EmployeeId} = ${fortnox_bi_dim_employees.EmployeeId}` (many-to-one)
- `fortnox_bi_fact_employee_wages` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_employee_wages.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_employee_wages` → `fortnox_bi_dim_employees` on `${fortnox_bi_fact_employee_wages.EmployeeId} = ${fortnox_bi_dim_employees.EmployeeId}` (many-to-one)
- `fortnox_bi_fact_incoming_goods` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_incoming_goods.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_incoming_goods` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_incoming_goods.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_incoming_goods` → `fortnox_bi_dim_stockpoints` on `${fortnox_bi_fact_incoming_goods.stockPointId} = ${fortnox_bi_dim_stockpoints.StockPointId}` (many-to-one)
- `fortnox_bi_fact_incoming_goods` → `fortnox_bi_dim_suppliers` on `${fortnox_bi_fact_incoming_goods.SupplierId} = ${fortnox_bi_dim_suppliers.SupplierId}` (many-to-one)
- `fortnox_bi_fact_incoming_goods` → `fortnox_bi_fact_purchase_orders` on `${fortnox_bi_fact_incoming_goods.purchaseOrderId} = ${fortnox_bi_fact_purchase_orders.purchaseOrderId}` (cardinality untested — omitted)
- `fortnox_bi_fact_invoice_accruals` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_invoice_accruals.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_invoice_accruals` → `fortnox_bi_fact_invoices` on `${fortnox_bi_fact_invoice_accruals.InvoiceId} = ${fortnox_bi_fact_invoices.InvoiceId}` (many-to-one)
- `fortnox_bi_fact_invoice_payments` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_invoice_payments.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_invoice_payments` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_invoice_payments.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_invoice_rows` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_invoice_rows.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_invoice_rows` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_invoice_rows.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_invoice_rows` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_invoice_rows.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_invoice_rows` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_invoice_rows.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_invoice_rows` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_fact_invoice_rows.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_fact_invoice_rows` → `fortnox_bi_fact_invoices` on `${fortnox_bi_fact_invoice_rows.InvoiceId} = ${fortnox_bi_fact_invoices.InvoiceId}` (many-to-one)
- `fortnox_bi_fact_invoices` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_invoices.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_invoices` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_invoices.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_invoices` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_fact_invoices.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_fact_locked_period` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_locked_period.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_offer_rows` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_offer_rows.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_offer_rows` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_offer_rows.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_offer_rows` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_offer_rows.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_offer_rows` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_offer_rows.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_offers` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_offers.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_offers` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_offers.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_order_rows` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_order_rows.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_order_rows` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_order_rows.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_order_rows` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_order_rows.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_order_rows` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_order_rows.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_orders` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_orders.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_orders` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_orders.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_orders` → `fortnox_bi_dim_stockpoints` on `${fortnox_bi_fact_orders.StockPointId} = ${fortnox_bi_dim_stockpoints.StockPointId}` (many-to-one)
- `fortnox_bi_fact_production_orders` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_production_orders.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_production_orders` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_production_orders.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_production_orders` → `fortnox_bi_dim_stockpoints` on `${fortnox_bi_fact_production_orders.StockPointId} = ${fortnox_bi_dim_stockpoints.StockPointId}` (many-to-one)
- `fortnox_bi_fact_purchase_orders` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_purchase_orders.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_purchase_orders` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_purchase_orders.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_purchase_orders` → `fortnox_bi_dim_customers` on `${fortnox_bi_fact_purchase_orders.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `fortnox_bi_fact_purchase_orders` → `fortnox_bi_dim_stockpoints` on `${fortnox_bi_fact_purchase_orders.stockPointId} = ${fortnox_bi_dim_stockpoints.StockPointId}` (many-to-one)
- `fortnox_bi_fact_purchase_orders` → `fortnox_bi_dim_suppliers` on `${fortnox_bi_fact_purchase_orders.SupplierId} = ${fortnox_bi_dim_suppliers.SupplierId}` (many-to-one)
- `fortnox_bi_fact_rolling_sum` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_rolling_sum.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_rolling_sum` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_fact_rolling_sum.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_fact_salary_transactions` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_salary_transactions.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_salary_transactions` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_salary_transactions.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_salary_transactions` → `fortnox_bi_dim_employees` on `${fortnox_bi_fact_salary_transactions.EmployeeId} = ${fortnox_bi_dim_employees.EmployeeId}` (many-to-one)
- `fortnox_bi_fact_salary_transactions` → `fortnox_bi_dim_expenses` on `${fortnox_bi_fact_salary_transactions.ExpenseId} = ${fortnox_bi_dim_expenses.ExpenseId}` (many-to-one)
- `fortnox_bi_fact_stockbalance` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_stockbalance.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_stockbalance` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_stockbalance.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_stockbalance` → `fortnox_bi_dim_stockpoints` on `${fortnox_bi_fact_stockbalance.stockPointId} = ${fortnox_bi_dim_stockpoints.StockPointId}` (many-to-one)
- `fortnox_bi_fact_stocktakings` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_stocktakings.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_stocktakings` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_stocktakings.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_stocktakings` → `fortnox_bi_dim_stockpoints` on `${fortnox_bi_fact_stocktakings.StockPointId} = ${fortnox_bi_dim_stockpoints.StockPointId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoice_accruals` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_supplier_invoice_accruals.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoice_rows` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_supplier_invoice_rows.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoice_rows` → `fortnox_bi_dim_articles` on `${fortnox_bi_fact_supplier_invoice_rows.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoice_rows` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_supplier_invoice_rows.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoice_rows` → `fortnox_bi_dim_suppliers` on `${fortnox_bi_fact_supplier_invoice_rows.SupplierId} = ${fortnox_bi_dim_suppliers.SupplierId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoices` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_supplier_invoices.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_supplier_invoices` → `fortnox_bi_dim_suppliers` on `${fortnox_bi_fact_supplier_invoices.SupplierId} = ${fortnox_bi_dim_suppliers.SupplierId}` (many-to-one)
- `fortnox_bi_fact_vouchers` → `fortnox_bi_dim_accounts` on `${fortnox_bi_fact_vouchers.AccountId} = ${fortnox_bi_dim_accounts.AccountId}` (many-to-one)
- `fortnox_bi_fact_vouchers` → `fortnox_bi_dim_company` on `${fortnox_bi_fact_vouchers.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_fact_vouchers` → `fortnox_bi_dim_financial_years` on `${fortnox_bi_fact_vouchers.FinancialYearId} = ${fortnox_bi_dim_financial_years.FinancialYearId}` (many-to-one)
- `fortnox_bi_time_reporting_registrations` → `fortnox_bi_dim_articles` on `${fortnox_bi_time_reporting_registrations.ArticleId} = ${fortnox_bi_dim_articles.ArticleId}` (many-to-one)
- `fortnox_bi_time_reporting_registrations` → `fortnox_bi_dim_company` on `${fortnox_bi_time_reporting_registrations.OrgId} = ${fortnox_bi_dim_company.OrgId}` (many-to-one)
- `fortnox_bi_time_reporting_registrations` → `fortnox_bi_dim_customers` on `${fortnox_bi_time_reporting_registrations.CustomerId} = ${fortnox_bi_dim_customers.CustomerId}` (many-to-one)
- `shopify_bi_fact_order_rows` → `shopify_bi_dim_articles` on `${shopify_bi_fact_order_rows.ArticleId} = ${shopify_bi_dim_articles.ArticleId}` (many-to-one)
- `shopify_bi_fact_order_rows` → `shopify_bi_fact_orders` on `${shopify_bi_fact_order_rows.OrderId} = ${shopify_bi_fact_orders.OrderId}` (many-to-one)
- `shopify_bi_fact_order_rows_staging` → `shopify_bi_dim_articles_staging` on `${shopify_bi_fact_order_rows_staging.ArticleId} = ${shopify_bi_dim_articles_staging.ArticleId}` (many-to-one)
- `shopify_bi_fact_order_rows_staging` → `shopify_bi_dim_customers_staging` on `${shopify_bi_fact_order_rows_staging.CustomerId} = ${shopify_bi_dim_customers_staging.CustomerId}` (many-to-one)
- `shopify_bi_fact_order_rows_staging` → `shopify_bi_fact_orders_staging` on `${shopify_bi_fact_order_rows_staging.OrderId} = ${shopify_bi_fact_orders_staging.OrderId}` (many-to-one)
- `shopify_bi_fact_orders` → `shopify_bi_dim_customers` on `${shopify_bi_fact_orders.CustomerId} = ${shopify_bi_dim_customers.CustomerId}` (many-to-one)
- `shopify_bi_fact_orders_staging` → `shopify_bi_dim_customers_staging` on `${shopify_bi_fact_orders_staging.CustomerId} = ${shopify_bi_dim_customers_staging.CustomerId}` (many-to-one)

