version: 2

sources:
  - name: app
    description: 'Data coming directly from Enhanza app'
    database: "{{ target.project | default(target.database) }}"
    schema: app_{{ var('uid') }}
    tables:
      - name: dimension_categories
      - name: dimension_mapping
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: CategoryId
          - name: DimensionId
          - name: OrgId
    
  - name: fortnox_api_demo
    description: 'Fortnox DEMO raw data'
    database: "{{ target.project | default(target.database) }}"
    schema: fortnox_api_{{ var('demo_uid', var('uid')) }}
    tables:
      - name: absence_causecodes
      - name: absence_transactions
      - name: accounts
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: Active
          - name: CostCenterSettings
          - name: Description
          - name: Number
          - name: ProjectSettings
          - name: Year
      - name: articles
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: Active
          - name: ArticleNumber
          - name: Bulky
          - name: DirectCost
          - name: FreightCost
          - name: Housework
          - name: OtherCost
          - name: PurchasePrice
          - name: QuantityInStock
          - name: ReservedQuantity
          - name: SalesPrice
          - name: StockGoods
          - name: StockValue
          - name: Type
          - name: Unit
          - name: VAT
      - name: assets
      - name: assets_types
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: Id
          - name: InUse
          - name: OrgId
          - name: Type
          - name: Url
      - name: attendance_transactions
      - name: budgets
      - name: company_settings
      - name: contracts
      - name: cost_centers
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: Code
      - name: customers
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: CustomerNumber
      - name: employees
      - name: expenses
      - name: financial_years
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: Id
      - name: incominggoods
      - name: invoice_accruals
      - name: invoice_payments
      - name: invoices
      - name: itemsuppliers
      - name: labels
      - name: lockedperiod
      - name: offers
      - name: orders
      - name: pricelists
      - name: prices
      - name: production_orders
      - name: projects
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: ProjectNumber
      - name: purchaseorders
      - name: salary_transactions
      - name: stockbalance
      - name: stockpoints
      - name: stocktakings
      - name: supplier_invoice_accruals
      - name: supplier_invoice_file_connections
      - name: supplier_invoices
      - name: suppliers
      - name: time_reporting_articles
      - name: time_reporting_registrations
      - name: v2_invoices
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: AdministrationFee
          - name: AdministrationFeeVAT
          - name: Balance
          - name: BasisTaxReduction
          - name: Booked
          - name: Cancelled
          - name: ContributionPercent
          - name: ContributionValue
          - name: Currency
          - name: CurrencyRate
          - name: CurrencyUnit
          - name: DocumentNumber
          - name: Freight
          - name: FreightVAT
          - name: Gross
          - name: HouseWork
          - name: InvoiceType
          - name: Net
          - name: NotCompleted
          - name: Reminders
          - name: RoundOff
          - name: Sent
          - name: TaxReduction
          - name: TaxReductionType
          - name: Total
          - name: TotalToPay
          - name: TotalVAT
          - name: VATIncluded
          - name: VoucherNumber
          - name: VoucherSeries
          - name: VoucherYear
          - name: WarehouseReady
      - name: v2_offers
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: AdministrationFee
          - name: AdministrationFeeVAT
          - name: BasisTaxReduction
          - name: Cancelled
          - name: ContributionPercent
          - name: ContributionValue
          - name: Currency
          - name: CurrencyRate
          - name: CurrencyUnit
          - name: DocumentNumber
          - name: Freight
          - name: FreightVAT
          - name: Gross
          - name: HouseWork
          - name: Net
          - name: NotCompleted
          - name: RoundOff
          - name: Sent
          - name: TaxReduction
          - name: TaxReductionType
          - name: Total
          - name: TotalToPay
          - name: TotalVAT
          - name: VATIncluded
      - name: v2_orders
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: AdministrationFee
          - name: AdministrationFeeVAT
          - name: BasisTaxReduction
          - name: Cancelled
          - name: ContributionPercent
          - name: ContributionValue
          - name: Currency
          - name: CurrencyRate
          - name: CurrencyUnit
          - name: DocumentNumber
          - name: Freight
          - name: FreightVAT
          - name: Gross
          - name: HouseWork
          - name: Net
          - name: NotCompleted
          - name: OrderType
          - name: RoundOff
          - name: Sent
          - name: TaxReduction
          - name: TaxReductionType
          - name: Total
          - name: TotalToPay
          - name: TotalVAT
          - name: VATIncluded
          - name: WarehouseReady
      - name: voucher_series
      - name: vouchers
        # Columns this project consumes from this table. Derived by scripts/dbt_column_memory.py
        # from the staging SQL that reads them, so this is a statement of what we depend on and
        # not an inventory of what the API returns. Removing one upstream is a breaking change.
        columns:
          - name: ReferenceType
          - name: VoucherNumber
          - name: VoucherSeries
          - name: Year
  
  - name: favrit_api_demo
    description: 'FAVRIT DEMO raw data'
    database: "{{ target.project | default(target.database) }}"
    schema: favrit_api_{{ var('demo_uid', var('uid')) }}
    tables:
      - name: user_location
      - name: orderline
      - name: accounting
      - name: ratings

