name: dim_contacts
table_reference:
  catalog: enhanza_sample
  schema: hubspot_bi_demo
  table: dim_contacts
columns:
- name: '[NEEDS INPUT] primary key'
  type: TEXT
  is_calculated: false
  not_null: true
  properties:
    dbt_tests: not_null,unique
    dbt_test_status: unknown
  is_primary_key: true
cached: false
properties:
  description: '[NEEDS INPUT] one row per <entity>. State the grain, not the model
    name.'
  dbt_layer: model
  dbt_unique_id: model.enhanza_hubspot.hubspot_bi_dim_contacts
  dbt_resource_type: model
primary_key: '[NEEDS INPUT] primary key'
