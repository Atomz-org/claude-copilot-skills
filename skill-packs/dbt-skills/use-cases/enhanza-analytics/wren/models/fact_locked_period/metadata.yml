name: fact_locked_period
table_reference:
  catalog: enhanza_sample
  schema: fortnox_bi_demo
  table: fact_locked_period
columns:
- name: OrgId
  type: TEXT
  is_calculated: false
  not_null: false
  properties:
    dbt_tests: relationships
    dbt_test_status: verified
cached: false
properties:
  description: Date before which all financial transactions are locked
  dbt_layer: model
  dbt_unique_id: model.enhanza_fortnox.fortnox_bi_fact_locked_period
  dbt_resource_type: model
