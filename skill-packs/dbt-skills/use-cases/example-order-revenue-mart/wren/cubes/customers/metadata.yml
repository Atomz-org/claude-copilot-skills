name: customers
base_object: dim_customers
measures:
- name: customer_count
  expression: "COUNT(customer_id)"
  type: BIGINT
dimensions:
- name: region
  expression: region
  type: VARCHAR
- name: country_code
  expression: country_code
  type: VARCHAR
time_dimensions:
- name: first_seen_at
  expression: first_seen_at
  type: TIMESTAMP
properties:
  description: "Customer dimension. One row per customer, current state."
  source: dbt_semantic_model
